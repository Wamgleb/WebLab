<?php
// перевіряємо, що форма була відправлена
if ($_SERVER["REQUEST_METHOD"] == "POST") {
  
  // отримуємо дані з форми
  $name = trim($_POST["name"]);
  $email = trim($_POST["email"]);
  $message = trim($_POST["message"]);

  // перевіряємо валідність електронної пошти
  if (!filter_var($email, FILTER_VALIDATE_EMAIL)) {
    // якщо електронна адреса недійсна, повертаємо помилку
    echo "Недійсна електронна адреса!";
  } else {
    // якщо дані валідні, обробляємо їх
    // наприклад, зберігаємо в базі даних або відправляємо на електронну пошту
    // і повідомляємо користувача про успішне відправлення повідомлення
    echo "Дякуємо за ваше повідомлення!";
  }
}
?>
